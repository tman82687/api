services:
  # A Docker web service
  - type: web
    name: webdis
    env: docker
    repo: https://github.com/tman82687/api.git
    plan: free
    branch: main
    envVars:
      - key: PORT
        value: 3000
